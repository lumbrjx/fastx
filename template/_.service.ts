//import { db } from "../../config/drizzle-client";
// import { parseToResult } from "result parser dir";
import { FooData } from "./_.model";
// import * as schema from "drizzle schema dir";

export async function fooService(user: FooData) {
  try {
    // return parseToResult(user);
  } catch (error: any) {
    // return parseToResult(undefined, error);
  }
}
